[phases.setup]
nixPkgs = []

[phases.install]
cmds = ["pnpm install --filter @workspace/api-server... --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/api-server run build"]

[start]
cmd = "node artifacts/api-server/dist/index.cjs"
